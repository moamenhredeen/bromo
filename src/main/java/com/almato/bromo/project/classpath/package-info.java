/// Classpath service — resolved jar/dir entries exposed to the compiler.
///
/// v0: a list-of-paths holder. Grows at M4 to support classpath lookups
/// (e.g. "which jar contains this binary type name?").
package com.almato.bromo.project.classpath;
