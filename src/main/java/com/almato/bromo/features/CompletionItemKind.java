package com.almato.bromo.features;

/// Kind of [CompletionItem] — maps to LSP `CompletionItemKind` in the adapter.
public enum CompletionItemKind {
    CLASS,
    INTERFACE,
    ENUM,
    RECORD,
    ANNOTATION,
    METHOD,
    CONSTRUCTOR,
    FIELD,
    VARIABLE,
    KEYWORD,
    SNIPPET,
}
